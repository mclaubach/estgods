<%= render :partial => 'matches/dotanav' %>
<div class="col-sm-10">
  error with dota2 match api preventing dota2 diary from getting user matches - will be fixed when API is stable once again
<% if @matches && @matches.any? %>
  <table class="table table-striped table-hover">
    <% @matches.each do |match| %>
      <tr>
        <td>
          <%= link_to match.started_at, match_path(match) %>
        </td>
      </tr>
    <% end %>
  </table>
<% end %>
</div>